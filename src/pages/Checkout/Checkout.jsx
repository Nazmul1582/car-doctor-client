import Hero from "../shared/Hero/Hero";

const Checkout = () => {
    return (
        <section>
            <Hero title="Check Out" route="Home/Checkout" />
        </section>
    );
};

export default Checkout;